name: Indulge

options:
  bundleIdPrefix: com.significanthobbies
  deploymentTarget:
    iOS: "18.0"
  createIntermediateGroups: true
  generateEmptyDirectories: true

settings:
  base:
    SWIFT_VERSION: "6.0"
    CODE_SIGN_STYLE: Automatic
    TARGETED_DEVICE_FAMILY: "1,2"
    ENABLE_USER_SCRIPT_SANDBOXING: YES

packages:
  PersonalSyncKit:
    url: https://github.com/Significant-Hobbies/significanthobbies.git
    revision: e350a86b2c255861f528d5a22bca1196d5a2898e

targets:
  Indulge:
    type: application
    platform: iOS
    sources:
      - path: Indulge
        excludes:
          - Resources/OnboardingStanding.png
    resources:
      - path: Indulge/Resources
    dependencies:
      - package: PersonalSyncKit
    settings:
      base:
        DEVELOPMENT_TEAM: 8F7LXHTJZR
        CODE_SIGN_ENTITLEMENTS: Indulge/Indulge.entitlements
        PRODUCT_BUNDLE_IDENTIFIER: com.significanthobbies.indulge
        PRODUCT_NAME: Indulge
        GENERATE_INFOPLIST_FILE: NO
        INFOPLIST_FILE: Indulge/Info.plist
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        CURRENT_PROJECT_VERSION: 8
        MARKETING_VERSION: 0.1.0
    scheme:
      testTargets:
        - IndulgeTests
        - IndulgeUITests
      gatherCoverageData: true

  IndulgeTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - path: IndulgeTests
    dependencies:
      - target: Indulge
      - package: PersonalSyncKit
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.significanthobbies.indulge.tests
        GENERATE_INFOPLIST_FILE: YES
        TEST_HOST: "$(BUILT_PRODUCTS_DIR)/Indulge.app/$(BUNDLE_EXECUTABLE_FOLDER_PATH)/Indulge"
        BUNDLE_LOADER: "$(TEST_HOST)"

  IndulgeUITests:
    type: bundle.ui-testing
    platform: iOS
    sources:
      - path: IndulgeUITests
    dependencies:
      - target: Indulge
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.significanthobbies.indulge.uitests
        GENERATE_INFOPLIST_FILE: YES
